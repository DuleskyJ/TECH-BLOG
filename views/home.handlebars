{{> layout}}

<h1>Welcome to the Tech Blog</h1>
{{#each posts}}
    <div class="post">
        <h2>{{title}}</h2>
        <p>{{content}}</p>
        <small>Posted by {{username}} on {{createdAt}}</small>
    </div>
{{/each}}