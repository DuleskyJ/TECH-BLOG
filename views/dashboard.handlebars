{{!-- Use the main layout --}}
{{!< main}}

<h1>Your Dashboard</h1>
{{#each posts}}
    <div class="post">
        <h2>{{title}}</h2>
        <p>{{content}}</p>
        <small>Posted by you on {{createdAt}}</small>
    </div>
{{/each}}